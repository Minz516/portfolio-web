# Task: Add New Project to Portfolio

Add a new project entry to the existing **Projects** section of the portfolio website (likely in `data/projects.ts` or similar typed data file). Follow the exact same card structure, styling, and animation already used for the other projects (EventX, Railway Management System, FlowGuard) — no new layout or component needed.

Place this project as the **most recent** entry (2026), so it should appear first if the list is ordered newest-first, or last if ordered chronologically oldest-first — match whatever ordering convention is already used for the existing projects.

---

## Project: TicTacToang

- **Role**: Fullstack Developer
- **Date**: 2026
- **Link**: GitHub Repo — `https://github.com/RMIT-Full-Stack-Development-2026A/Group1.git`
- **Tags**: React, Node.js, WebSocket

**Description**:
A real-time multiplayer Tic-Tac-Toe web application with a React frontend and Node.js backend, using WebSocket-based communication to power live, synchronized online matches between players. Built end-to-end across both frontend and backend with a clean, modular architecture designed for scalability and maintainability — a hands-on deep dive into full-stack system design and professional software engineering practices.

---

## Implementation Notes

- Do not modify or reorder any existing project entries.
- Use a placeholder/gradient visual for the project thumbnail if no screenshot is available (consistent with how other projects without real screenshots are already handled).
- Keep copy consistent in tone with the other project descriptions already in the data file.
- After the change, add a short line to `NOTES.md` noting this project was added.
